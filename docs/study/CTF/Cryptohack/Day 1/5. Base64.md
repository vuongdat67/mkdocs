---
platform: CryptoHack
category: crypto
challenge: Base64
tags:
  - crypto
  - cryptohack
  - 2025
status: in-progress
created: 2025-07-23
---

## 🧩 Challenge name – Category


## 📝 Mô tả đề bài

*(Dán đề bài hoặc tóm tắt nội dung chính ở đây)*
Another common encoding scheme is Base64, which allows us to represent binary data as an ASCII string using an alphabet of 64 characters. One character of a Base64 string encodes 6 binary digits (bits), and so 4 characters of Base64 encode three 8-bit bytes.  
  
Base64 is most commonly used online, so binary data such as images can be easily included into HTML or CSS files.  
  
Take the below hex string, _decode_ it into bytes and then _encode_ it into Base64.  
  
72bca9b68fc16ac7beeb8f849dca1d8a783e8acf9679bf9269f7bf  
  
 In Python, after importing the base64 module with `import base64`, you can use the `base64.b64encode()` function. Remember to decode the hex first as the challenge description states.

---

## 🧠 Ý tưởng giải

- Phân tích dạng bài
- Công thức / thuật toán áp dụng
- Gợi ý (nếu có)

Chuyển từ hex --> bytes
Chuyển từ bytes --> base64

---

## 🔍 Cách giải từng bước

```python
import base64
# step 1: hex to bytes
hex_string = "72bca9b68fc16ac7beeb8f849dca1d8a783e8acf9679bf9269f7bf"
bytes_data = bytes.fromhex(hex_string)
# step 2: bytes to base64
base64_encoded = base64.b64encode(bytes_data)
# print result
print(base64_encoded.decode())
```

---

## 🎯 Flag
crypto/Base+64+Encoding+is+Web+Safe/

---

## 📚 Ghi chú thêm

- Kiến thức cần nhớ:
    
	- `bytes.fromhex()` biến chuỗi hex thành dạng `bytes`
    
	- `base64.b64encode()` biến `bytes` thành base64 dạng `bytes`
    
	- `.decode()` biến kết quả về `str` để dễ in ra màn hình
        
	- Liên quan đến dạng bài khác:
    
    - ...
        

---

## 🔗 Link

🔗 [[]]